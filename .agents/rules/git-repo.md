---
trigger: always_on
---

---

description: Apply whenever Git operations, repository management, or remote syncing is discussed
glob: "*"
---

# Git Repository Context

- This directory is a local Git repository.
- The remote repository (origin) URL is explicitly: `https://github.com/kl-2d/niva_service.git`
- Whenever performing operations with the remote repository (such as push, pull, fetch, or setting remote), use this exact URL by default.
- Do not ask the user for the repository URL.
